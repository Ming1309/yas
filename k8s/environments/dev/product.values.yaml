backend:
  image:
    repository: docker.io/mingpham/yas-product
    tag: main

  service:
    type: ClusterIP

  serviceMonitor:
    enabled: true
    path: /product/actuator/prometheus

  resources:
    requests:
      cpu: 100m
      memory: 384Mi
    limits:
      cpu: 700m
      memory: 768Mi
