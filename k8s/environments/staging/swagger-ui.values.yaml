image:
  repository: swaggerapi/swagger-ui
  tag: v5.17.14

service:
  type: ClusterIP

ingress:
  enabled: false

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
