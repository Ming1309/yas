# Demo marker for validating the dev auto-deploy flow.
# Changing a file under tax/** triggers tax CI, image build, GitOps dev update,
# and ArgoCD sync into the yas-dev namespace after the PR is merged.
demo.dev.auto-deploy=enabled
