apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: yas-staging-swagger-ui
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Ming1309/yas.git
    targetRevision: main
    path: k8s/charts/swagger-ui
    helm:
      valueFiles:
        - ../../environments/staging/swagger-ui.values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: yas-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
